/**
 * External destinations for the icon directory. This site only exists to bring
 * traffic to the main product, so we link out rather than claim anything here.
 */
export const LINKS = {
	/** The main CodeInOven product site. */
	product: 'https://codeinoven.com',
	/** CodeInOven's GitHub repository. */
	github: 'https://github.com/pillardash/codeinoven',
	/** The main product's download page. */
	download: 'https://codeinoven.com/download'
} as const;
